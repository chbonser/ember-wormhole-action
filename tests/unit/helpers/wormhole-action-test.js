
import { wormholeAction } from 'dummy/helpers/wormhole-action';
import { module, test } from 'qunit';

module('Unit | Helper | wormhole action');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wormholeAction([42]);
  assert.ok(result);
});

